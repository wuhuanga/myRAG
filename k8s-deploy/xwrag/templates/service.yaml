apiVersion: v1
kind: Service
metadata:
  name: {{ include "xwrag.fullname" . }}
  labels:
    {{- include "xwrag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.env.PORT }}
      protocol: TCP
      name: http
  selector:
    {{- include "xwrag.selectorLabels" . | nindent 4 }}
